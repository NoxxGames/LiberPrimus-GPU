# Stage 1D p56 Prime-Stream Golden Fixture Research Log

Stage 1D reproduces the known p56 `An End` solved material as a fixture baseline.

The method reference documents `phi(prime)` shifted down through forward Gematria with cleartext-F skip handling. For prime inputs, `phi(p)=p-1`, so this stage treats `phi_prime_stream` as an alias for `prime_minus_one_stream`.

The selected Stage 0E candidate span is logical lines `1690..1710`. The hex payload occupies logical lines `1697..1705` and is preserved separately with SHA-256 `45622e92614e822640345088530b674d0ec2c1cb3a6887271ed9734c5ef00885`.

The expected normalized plaintext uses Gematria profile labels, so conventional `K` is represented by `C` and conventional `V` by `U`. The cleartext-F skip is fixture-declared at token `22202`.

This is a known-solved baseline only. It does not activate a canonical corpus, finalize page boundaries, implement search, or touch CUDA.
