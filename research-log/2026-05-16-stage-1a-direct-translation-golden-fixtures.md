# Stage 1A Research Log

Stage 1A introduces solved-page golden fixtures for direct translation only. Four reviewable direct spans from the rtkd corpus candidate are reproduced using Gematria profile v0. The expected normalized output uses profile-preferred labels and is therefore a tooling expectation, not editorial plaintext and not a solve claim.

The fixture framework keeps `canonical_corpus_active=false`, `page_boundaries_final=false`, and `trusted_as_canonical=false`. Generated reproduction records are ignored and may be regenerated from committed fixtures and locked sources.
