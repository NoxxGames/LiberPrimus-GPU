# Stage 5T Solved-Family Inventory Summary

Stage 5T inventories eight solved-family or solved-fixture-safe CUDA-relevant surfaces.

The committed inventory includes direct translation, reverse Gematria, rotated reverse Gematria, explicit-key Vigenere, prime-minus-one stream, Gematria shift-score-only, the Stage 5F synthetic shift-score surface, and the Stage 5J synthetic Gematria mod-29 surface.

The inventory explicitly separates current-kernel `gematria_shift_score_only` parity from original transform-family semantics. Direct translation fixtures can feed token-buffer parity, but direct translation itself is not marked as CUDA-verified.
