# Stage 5BB Manifest Precedence Summary

Stage 5BB canonicalised active manifest precedence for future token-block preflight work.

Active:

- Stage 5AW repaired branch manifest.
- Stage 5AY branch-eligibility policy.
- Stage 5AZ repaired preflight design policy.
- Stage 5AZ repaired bounded variant-family manifest.
- Stage 5AZ repaired branch budget and execution gates.

Inactive as active inputs:

- Stage 5AV token variant branch manifest.
- Stage 5AY bounded variant-family manifest.

The Stage 5AZ execution-gate legacy pointer to the old Stage 5AY bounded variant-family manifest is classified as `legacy_superseded_pointer`. Historical diagnostic loading is allowed only when explicit; active loading is blocked.
