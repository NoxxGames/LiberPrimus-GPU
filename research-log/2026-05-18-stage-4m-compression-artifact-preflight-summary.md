# Stage 4M Compression Artefact Preflight Summary

Stage 4M produced deterministic metric-only compression preflight records for 58 ignored local LP page images.

Each record includes file metadata, SHA-256, an 8x8 blockiness proxy, edge/noise residual summaries, channel histogram summaries, and grayscale bitplane ratios. The local image inventory is JPEG, so all 58 records carry a JPEG-like metric flag. This is a format/metric flag only; it is not evidence that a visual artefact is intentional.

The Stage 4E JPEG-like/star-like observation remains a review-only artifact candidate. Source variants and ordinary compression/null controls are required before any future interpretation.
