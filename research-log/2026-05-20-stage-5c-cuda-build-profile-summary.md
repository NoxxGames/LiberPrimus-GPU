# Stage 5C CUDA Build Profile Summary

Stage 5C records three build profiles:

- no-GPU CI profile;
- compatibility 8 GB profile;
- optional local 16 GB profile.

The local 16 GB profile is not required by CI or future execution. These records are build/readiness metadata only and do not authorize CUDA kernels or speedup claims.
