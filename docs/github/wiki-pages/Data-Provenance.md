# Data Provenance

Raw files are not committed. Local raw sources are hash-locked with SHA-256 metadata when present.

Non-canonical sources may support parser tests and alignment hints. They cannot prove an unsolved plaintext or activate corpus records.

Generated outputs are ignored and should be regenerated from locked raw sources and code.
