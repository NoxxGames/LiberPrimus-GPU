# Running Workbook Tools

The legacy workbook `tranlsations.xlsx` is non-canonical.

Example:

```powershell
libreprimus legacy-workbook summary --workbook <path-to-local-xlsx>
```

Generated workbook outputs stay under ignored normalized-data folders.
