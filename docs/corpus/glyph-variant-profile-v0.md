# Glyph Variant Profile v0

## Status

Frozen tooling profile. `canonical_profile_active=true`; `canonical_corpus_active=false`.

## Profile ID

`glyph-variants-v0`

## File Path

`data/profiles/glyph-variants/glyph-variants-v0.json`

## SHA-256

`5acae61c4ea2aa9f2f2fb76bdcafb7ed9c6504bd98caf29590a95d7d43271d6d`

## `ᛂ` Evidence

Stage 0D-followup preserves raw `ᛂ` and records a normalized-view candidate to `ᛄ` from observed prime value `37`, inferred index `11`, and alignment support.

## Raw Preservation

Raw source glyphs must be preserved in generated records.

## Normalized-View-Only Rule

The normalized view may map `ᛂ -> ᛄ` only when evidence is recorded and output records set `variant_mapping_applied=true`.

## No Canonical Mapping Change

This profile does not add `ᛂ` to Gematria profile v0 and does not change canonical index order.

## Validation Rules

Every normalized candidate must exist in Gematria profile v0, every variant must preserve raw-glyph policy, and `canonical_mapping_change` must be false.
