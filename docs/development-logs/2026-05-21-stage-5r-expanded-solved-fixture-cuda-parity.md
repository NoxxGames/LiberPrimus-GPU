# Stage 5R Expanded Solved-Fixture CUDA Parity

Starting commit: `b8933d6409280d405bdbe487ec5747116c38ae1b`

Stage 5R implemented the controlled expanded solved-fixture-safe Gematria CUDA parity workflow for the exact three Stage 5Q mapped direct-translation candidates.

Work completed:

- added `libreprimus gematria-expanded-solved-fixture-cuda` CLI commands;
- added run, parity, boundary, result-store preflight, score-summary preflight, and summary schemas;
- generated committed compact YAML records under `data/cuda/`;
- ran the existing `gematria_mod29_shift_score_kernel` locally over exactly `p57-parable`, `some-wisdom`, and `the-loss-of-divinity`;
- matched all three CUDA output-token hashes to Stage 5Q native hashes;
- added no CUDA kernels and made no CUDA source or device arithmetic changes;
- added no benchmark, speedup claim, unsolved-page CUDA, raw data processing, generated-body publication, website expansion, canonical corpus activation, page-boundary finalisation, or solve claim.

Generated detailed reports remain ignored under `experiments/results/gematria-expanded-solved-fixture-cuda/stage5r/`. The detailed Codex handoff is written under ignored `codex-output/`.
