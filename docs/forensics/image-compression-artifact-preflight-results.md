# Image Compression Artefact Preflight Results

Stage 4M computed deterministic metric-only records for the local ignored LP page images.

## Local Summary

- LP images scanned: `58`.
- Source-variant records: `58`.
- Compression metric records: `58`.
- Artifact review candidates: `1`.
- Images with JPEG-like metric flags: `58`.
- Source variants blocked because external variant bytes are not cached: `58`.
- Bigram/Fibonacci-421 readiness records: `1`, blocked.

The JPEG-like flags are expected because the local LP page inventory is JPEG. The flag is not an intentionality claim and not hidden-message evidence.

## Metrics

The committed compression preflight records include:

- an 8x8 grid blockiness proxy;
- local variance/noise residual summary;
- edge residual summary;
- channel histogram summary;
- grayscale bitplane one-ratio summary.

No residual images, bitplane images, contact sheets, or visualisations are committed.

## Interpretation Boundary

Stage 4M does not infer that star-like or compression-like features are meaningful. Any future claim needs source variants, null/negative controls, and explicit review promotion before a manifest can even be planned.
