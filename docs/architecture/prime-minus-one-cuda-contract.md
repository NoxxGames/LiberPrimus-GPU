# Prime-Minus-One CUDA Contract

Stage 5Z prepares the prime-minus-one CUDA contract from Stage 5Y reporting metadata. It records token domain, stream formula, separator policy, output ordering, result-store compatibility, full-p56 blockers, and no-execution guardrails.

This is contract preparation only. It does not modify CUDA source, add kernels, run native parity, run CUDA, benchmark, publish generated result bodies, process raw data, upgrade method families, or make solve claims.

The future implementation scope selected by Stage 5Z is `Stage 5AA - prime-minus-one CUDA synthetic kernel implementation and parity`. That future stage must remain synthetic-only unless its prompt explicitly widens the scope.
