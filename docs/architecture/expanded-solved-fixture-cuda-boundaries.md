# Expanded Solved-Fixture CUDA Boundaries

Stage 5R execution is limited to `exact_three_stage5q_mapped_direct_translation_candidates_only`.

Boundary records must show:

- consumed Stage 5L/5M/5O controls are excluded as new Stage 5R inputs;
- blocked original transform-family fixtures remain excluded;
- original direct-translation semantics are not exercised by the CUDA kernel;
- no unsolved-page CUDA or raw Liber Primus CUDA data is used;
- no GPU benchmark, speedup, or performance claim is made;
- no generated result body, raw data, or `codex-output/**` file is committed.

Parity success can move the work to compact Stage 5S reporting and result-store integration. It cannot widen CUDA scope or upgrade any method family to solved.
