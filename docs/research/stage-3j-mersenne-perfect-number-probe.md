# Stage 3J Mersenne / Perfect-Number Probe Research Note

The Mersenne/perfect-number probe is a weak-to-moderate adjacent hypothesis. It is justified only as a cheap falsification test because earlier project evidence already includes prime-derived values and binary-style motifs.

Stage 3J keeps the test tiny:

- no external exponent lists;
- no arbitrary number sequences;
- no broad OEIS-style campaign;
- no CUDA;
- no solve claim.

The bounded run did not produce a strong result. The top candidate calibrated as `inconclusive` with a raw `garbage` label. The result supports moving to reviewable visual numeric observations or archive-source audit work before adding more numeric seeds.
