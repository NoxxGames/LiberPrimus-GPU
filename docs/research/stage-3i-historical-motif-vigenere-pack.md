# Stage 3I Historical Motif Vigenere Pack Research Note

Stage 3I tests a small historical motif key pack because earlier Cicada material used book, author, and phrase motifs. This evidence is weaker than LP-local solved-page evidence, so the run remains bounded and conservative.

The experiment tests exactly `56` candidates:

- `14` declared historical keys
- `2` reset modes: `none`, `line`
- `2` advance modes: `runes_only`, `token_break_preserving`

The top candidate was `SELFRELIANCE` with reset `line`, advance `runes_only`, and calibrated confidence `noisy`. This is a lead only. It is not a solve claim and does not justify expanding into dictionary search.

Stage 3I keeps generated outputs local and ignored. Future image or visual numeric observations should be captured as reviewable observations before being converted into transform seeds.

