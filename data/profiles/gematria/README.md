# Gematria Profiles

Frozen Gematria profiles define rune, index, prime, and display-label mappings used by tooling.

Stage 0E freezes `gematria-primus-v0` for tooling while keeping `canonical_corpus_active=false`.
