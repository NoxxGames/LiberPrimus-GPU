# Liber Primus Page Image Locks

This directory stores committed lock records for local third-party Liber Primus page images. The lock records contain filenames, paths, file sizes, hashes, and deterministic image metadata only.

Raw page image files remain local under `third_party/LiberPrimusPages/` and must not be committed.
