# Source Snapshot Locks

Stage 4K records reproducible public-source metadata for a small allowlisted
set of sources. These records preserve URLs, retrieval metadata, hashes where
content was fetched, copyright notes, snapshot policy, and source-lock status.

The directory is metadata-only by default. It must not contain raw binary,
image, audio, font, archive, Discord, or broad mirrored repository content.
