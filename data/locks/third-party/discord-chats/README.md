# Discord Chat Archive Locks

This directory stores committed aggregate lock summaries for local admin-provided Discord HTML exports.

Raw HTML files remain ignored under `third_party/LiberPrimusDiscordChats/`. File-level lock details are generated outputs unless explicitly reduced to aggregate, privacy-preserving summaries.
