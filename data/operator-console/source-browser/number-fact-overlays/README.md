# Number-fact enrichment overlays

Committed overlays may add display/review context for existing Source Browser number facts without rewriting historical source-lock records. Files under `templates/` are examples and are ignored by the live loader.
