# Vigenere Solved Fixtures v0

This directory contains committed fixture metadata for known solved explicit-key Vigenere sections.

Fixtures are test expectations, not new solve claims. They must declare keys and skip rules explicitly, preserve source/profile provenance, keep `canonical_corpus_active=false`, and write generated reproduction outputs under ignored solved-baseline paths.
