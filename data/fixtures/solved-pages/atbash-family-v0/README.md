# Atbash-Family Solved Fixtures v0

This directory contains committed golden-fixture metadata for known solved reverse Gematria and rotated reverse Gematria sections.

Fixtures are test expectations, not new solve claims. They preserve source/profile SHA-256 provenance, keep `canonical_corpus_active=false`, and use reviewable spans from the generated Stage 0E corpus candidate.

Generated reproduction outputs belong under `data/normalized/solved-baselines/atbash-family-v0/` and must not be committed.
