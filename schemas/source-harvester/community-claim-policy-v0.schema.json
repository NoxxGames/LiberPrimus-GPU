{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Stage 5AK Community Claim Policy",
  "type": "object",
  "required": ["record_type", "stage_id", "source_stage_id", "source_id", "required_fields", "execution_ready", "solve_claim", "website_publication_allowed", "deep_research_private_allowed"],
  "properties": {
    "record_type": { "const": "stage5ak_community_claim_policy" },
    "stage_id": { "const": "stage-5ak" },
    "source_stage_id": { "const": "stage-5aj" },
    "source_id": { "const": "community_facts_observations_local" },
    "required_fields": { "type": "array", "items": { "type": "string" } },
    "execution_ready": { "const": false },
    "solve_claim": { "const": false },
    "website_publication_allowed": { "const": false },
    "deep_research_private_allowed": { "const": true }
  },
  "additionalProperties": true
}
