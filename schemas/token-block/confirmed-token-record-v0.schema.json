{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "properties": {
    "canonical_transcription_changed": { "const": false },
    "record_type": { "const": "confirmed_token_record_set" },
    "schema": { "const": "schemas/token-block/confirmed-token-record-v0.schema.json" },
    "solve_claim": { "const": false },
    "stage_id": { "const": "stage-5av" }
  },
  "required": [
    "record_type",
    "schema",
    "stage_id",
    "confirmed_token_count",
    "records",
    "canonical_transcription_changed",
    "solve_claim"
  ],
  "type": "object"
}
