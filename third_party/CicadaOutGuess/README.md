# Cicada OutGuess Regression Artefacts

This ignored directory is for optional local OutGuess-specific fixture artefacts, including manually source-locked known positives and known negatives.

Only this README and `.gitkeep` are intended to be committed. Raw artefacts and extracted payloads must remain local and ignored.
