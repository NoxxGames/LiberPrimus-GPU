# Local Liber Primus Page Images

This directory is for local original Liber Primus page image files.

Image files are intentionally ignored. Use the Stage 3K source-lock CLI to hash and record deterministic metadata before any observation is promoted into the review registry.

Do not commit raw images unless project policy changes.
