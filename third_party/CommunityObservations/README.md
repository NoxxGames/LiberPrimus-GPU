# Community observation artefacts

This ignored cache is for local screenshots, attachment copies, and metadata supplied for reviewable community observations. Commit only this README and marker files; raw images and generated metadata stay ignored unless a future source policy explicitly promotes an aggregate record.
