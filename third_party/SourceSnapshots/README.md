# SourceSnapshots

Ignored local cache for Stage 4K allowlisted public source-lock fetches.

Only this README and `.gitkeep` are tracked. Cached public HTML/text bytes,
download reports, binary artefacts, images, audio, fonts, archives, and copied
third-party content must remain uncommitted unless a later explicit policy
approves a small text snapshot path.
