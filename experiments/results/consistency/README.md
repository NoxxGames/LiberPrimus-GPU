# Consistency Results

Generated consistency-check summaries are written here for local review only.

The JSON summaries are ignored by Git. Only this README and `.gitkeep` files are
committed so the output locations are visible.
