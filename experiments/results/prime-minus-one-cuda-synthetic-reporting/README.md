# Prime-Minus-One CUDA Synthetic Reporting Outputs

Stage 5AC generated reports are written under this ignored tree. Commit only this README and `.gitkeep` allowlist files; JSON reports and result bodies remain local generated outputs.
