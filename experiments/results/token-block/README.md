Generated token-block source-lock and preflight outputs live here.

Only this README and `.gitkeep` scaffolds are committed. Stage-specific reports,
CSV/JSON/JSONL outputs, and any generated review material remain ignored.
