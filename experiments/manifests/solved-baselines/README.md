# Solved-Baseline Manifests

These manifests run known solved fixture groups through the CPU reference transform registry.

They are regression manifests, not search-campaign manifests, and they must not contain raw corpus dumps.
