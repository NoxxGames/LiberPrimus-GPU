# Stage 4E Disabled Future Manifests

These manifests are queued for future source-lock and deterministic image/stego provenance work. They are disabled and must not be executed without a later explicit stage.
